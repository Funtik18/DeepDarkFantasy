using DDFInventory.Utility;
using System.Collections;
using System.Collections.Generic;
using Toolbox.Common;
using UnityEngine;
using UnityEngine.EventSystems;

public class UIMouseFollower : MonoBehaviour {

    /*public Canvas canvas;

    private CoroutineObject routine;

	private void Awake() {
        routine = new CoroutineObject(this, Routine);
	}

    public void Go() => routine.Start();

    public void Stop() => routine.Stop();

    private IEnumerator Routine() {
		while (true) {
            transform.position = CanvasMouseFollower.GetPointerPosOnCanvas(canvas, PointerUtility.GetPosition());
            yield return null;
        }
    }

	private void OnMouseDown() {
        Go();
    }
	private void OnMouseDrag() {

	}
	private void OnMouseUp() {
        Stop();
    }*/
}
